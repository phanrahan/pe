from setuptools import setup
import sys

setup(
    name='pe',
    version='0.1-alpha',
    description='Executable specification for the CGRA PE',
    scripts=[],
    packages=[
        "pe",
    ],
    python_requires='>=3.6'
)
